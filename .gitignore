node_modules/
.next/
out/
.env*.local
*.tsbuildinfo
next-env.d.ts

# Verification artefacts — regenerated by `npm run devanagari:verify`
scripts/.verify/
scripts/.shots/

# The photographs themselves. `npm run images` turns these into the committed
# derivatives in public/img/ — the originals are large, private, and would be
# in the repository forever.
assets/photos/*
!assets/photos/README.md
